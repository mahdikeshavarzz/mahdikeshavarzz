import { createMuiTheme } from "@material-ui/core";

const Theme = createMuiTheme({
  typography: {
    fontFamily: "cursive",
  },
  root: {
    fontFamily: "cursive",
  },
});

export default Theme;